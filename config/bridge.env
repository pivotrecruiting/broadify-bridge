# Bridge production defaults – used as seed when userData/.env is missing.
# Edit userData/.env after first launch to customize (relay, ports, etc.).

# Graphics – Native Display Helper + FrameBus (required for display output)
BRIDGE_GRAPHICS_RENDERER=electron
BRIDGE_GRAPHICS_RENDERER_SINGLE=1
BRIDGE_GRAPHICS_FRAMEBUS=1
BRIDGE_GRAPHICS_OUTPUT_HELPER_FRAMEBUS=1
BRIDGE_FRAMEBUS_NAME=broadify-framebus
BRIDGE_DISPLAY_NATIVE_HELPER=1

# File Reference – src/ui/App.tsx

## Zweck
Haupt‑UI für Bridge‑Konfiguration (Network/Port) und Start/Stop‑Flow.

## Ein-/Ausgänge
- Input: UI‑State + IPC‑Ergebnisse
- Output: UI‑Actions via `window.electron.*`

## Abhängigkeiten
- Hooks: `src/ui/hooks/*`
- Komponenten: `src/ui/components/*`
